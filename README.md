# Formacao Dev - Landing Page

## Run locally
- Open `index.html` in a browser.
- Optional: serve static files with `python -m http.server` and visit `http://localhost:8000`.

## Deploy
- Upload `index.html`, `styles.css`, and `script.js` to any static hosting (Netlify, Vercel, GitHub Pages).
